// This file was auto-generated by Fern from our API Definition.

import type * as Payroc from "../../../../../../index.js";

/**
 * @example
 *     {
 *         processingTerminalId: "1234001",
 *         orderId: "OrderRef6543",
 *         operator: "Jane",
 *         cardholderName: "Sarah%20Hazel%20Hopper",
 *         first6: "453985",
 *         last4: "7062",
 *         tender: "ebt",
 *         dateFrom: "2024-07-01T15:30:00Z",
 *         dateTo: "2024-07-03T15:30:00Z",
 *         settlementState: "settled",
 *         settlementDate: "2024-07-02",
 *         paymentLinkId: "JZURRJBUPS",
 *         before: "2571",
 *         after: "8516",
 *         limit: 1
 *     }
 *
 * @example
 *     {
 *         processingTerminalId: "1234001",
 *         orderId: "OrderRef6543",
 *         operator: "Jane",
 *         cardholderName: "Sarah%20Hazel%20Hopper",
 *         first6: "453985",
 *         last4: "7062",
 *         tender: "ebt",
 *         dateFrom: "2024-07-01T15:30:00Z",
 *         dateTo: "2024-07-03T15:30:00Z",
 *         settlementState: "settled",
 *         settlementDate: "2024-07-02",
 *         paymentLinkId: "JZURRJBUPS",
 *         before: "2571",
 *         after: "8516",
 *         limit: 1
 *     }
 */
export interface ListPaymentsRequest {
    /** Filter by terminal ID. */
    processingTerminalId?: string;
    /** Filter payments by order ID. */
    orderId?: string;
    /** Filter payments by operator. */
    operator?: string;
    /** Filter payments by the cardholder’s name. */
    cardholderName?: string;
    /** Filter payments by the first six digits of the card number that the customer used in the transaction. */
    first6?: string;
    /** Filter payments by the last four digits of the card number that the customer used in the transaction. */
    last4?: string;
    /** Filter by tender type. */
    tender?: Payroc.cardPayments.ListPaymentsRequestTender;
    /** Filter payments by tip. */
    tipMode?: Payroc.cardPayments.ListPaymentsRequestTipModeItem | Payroc.cardPayments.ListPaymentsRequestTipModeItem[];
    /** Filter payments by transaction type. */
    type?: Payroc.cardPayments.ListPaymentsRequestTypeItem | Payroc.cardPayments.ListPaymentsRequestTypeItem[];
    /** Filter payments by the status of the transaction. */
    status?: Payroc.cardPayments.ListPaymentsRequestStatusItem | Payroc.cardPayments.ListPaymentsRequestStatusItem[];
    /** Filter by payments that the processor processed after a specific date. The date format follows the ISO 8601 standard. */
    dateFrom?: string;
    /** Filter by payments that the processer processed before a specific date. The date format follows the ISO 8601 standard. */
    dateTo?: string;
    /** Filter payments by the settlement status of the transaction. */
    settlementState?: Payroc.cardPayments.ListPaymentsRequestSettlementState;
    /** Filter by payments that the processor settled on a specific date in the format **YYYY-MM-DD**. */
    settlementDate?: string;
    /** Unique identifier that our gateway assigned to the payment link. */
    paymentLinkId?: string;
    /**
     * Return the previous page of results before the value that you specify.
     *
     * You can’t send the before parameter in the same request as the after parameter.
     */
    before?: string;
    /**
     * Return the next page of results after the value that you specify.
     *
     * You can’t send the after parameter in the same request as the before parameter.
     */
    after?: string;
    /** Limit the maximum number of results that we return for each page. */
    limit?: number;
}
