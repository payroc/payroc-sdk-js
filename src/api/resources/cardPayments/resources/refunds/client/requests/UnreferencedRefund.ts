// This file was auto-generated by Fern from our API Definition.

import type * as Payroc from "../../../../../../index.js";

/**
 * @example
 *     {
 *         "Idempotency-Key": "8e03978e-40d5-43e8-bc93-6894a57f9324",
 *         channel: "pos",
 *         processingTerminalId: "1234001",
 *         order: {
 *             orderId: "OrderRef6543",
 *             description: "Refund for order OrderRef6543",
 *             amount: 4999,
 *             currency: "USD"
 *         },
 *         refundMethod: {
 *             type: "card",
 *             cardDetails: {
 *                 entryMethod: "raw",
 *                 device: {
 *                     model: "bbposChp",
 *                     serialNumber: "1850010868"
 *                 },
 *                 rawData: "A1B2C3D4E5F67890ABCD1234567890ABCDEF1234567890ABCDEF1234567890ABCDEF"
 *             }
 *         },
 *         customFields: [{
 *                 name: "yourCustomField",
 *                 value: "abc123"
 *             }]
 *     }
 */
export interface UnreferencedRefund {
    /** Unique identifier that you generate for each request. You must use the [UUID v4 format](https://www.rfc-editor.org/rfc/rfc4122) for the identifier. For more information about the idempotency key, go to [Idempotency](https://docs.payroc.com/api/idempotency). */
    "Idempotency-Key": string;
    /** Channel that the merchant used to request the refund. */
    channel: UnreferencedRefund.Channel;
    /** Unique identifier that we assigned to the terminal. */
    processingTerminalId: string;
    /** Operator who initiated the request. */
    operator?: string;
    order: Payroc.RefundOrder;
    customer?: Payroc.Customer;
    ipAddress?: Payroc.IpAddress;
    /** Object that contains information about how the merchant refunds the customer. */
    refundMethod: Payroc.cardPayments.UnreferencedRefundRefundMethod;
    /** Array of customField objects. */
    customFields?: Payroc.CustomField[];
}

export namespace UnreferencedRefund {
    /** Channel that the merchant used to request the refund. */
    export const Channel = {
        Pos: "pos",
        Moto: "moto",
    } as const;
    export type Channel = (typeof Channel)[keyof typeof Channel];
}
