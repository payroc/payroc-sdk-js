// This file was auto-generated by Fern from our API Definition.

import type { BaseClientOptions, BaseRequestOptions } from "../../../../../../BaseClient.js";
import { type NormalizedClientOptionsWithAuth, normalizeClientOptionsWithAuth } from "../../../../../../BaseClient.js";
import { mergeHeaders } from "../../../../../../core/headers.js";
import * as core from "../../../../../../core/index.js";
import * as environments from "../../../../../../environments.js";
import { handleNonStatusCodeError } from "../../../../../../errors/handleNonStatusCodeError.js";
import * as errors from "../../../../../../errors/index.js";
import * as Payroc from "../../../../../index.js";

export declare namespace FundingActivityClient {
    export type Options = BaseClientOptions;

    export interface RequestOptions extends BaseRequestOptions {}
}

export class FundingActivityClient {
    protected readonly _options: NormalizedClientOptionsWithAuth<FundingActivityClient.Options>;

    constructor(options: FundingActivityClient.Options) {
        this._options = normalizeClientOptionsWithAuth(options);
    }

    /**
     * Use this method to return a [paginated](https://docs.payroc.com/api/pagination) list of funding balances available for each merchant linked to your account.
     *
     * Use query parameters to filter the list of results we return, for example, to search for the funding balance for a specific merchant.
     *
     * Our gateway returns the following information about each merchant in the list:
     * - Total funds for the merchant.
     * - Available funds that you can use for funding instructions.
     * - Pending funds that we have not yet sent to funding accounts.
     *
     * @param {Payroc.funding.RetrieveBalanceFundingActivityRequest} request
     * @param {FundingActivityClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Payroc.BadRequestError}
     * @throws {@link Payroc.UnauthorizedError}
     * @throws {@link Payroc.ForbiddenError}
     * @throws {@link Payroc.NotAcceptableError}
     * @throws {@link Payroc.InternalServerError}
     *
     * @example
     *     await client.funding.fundingActivity.retrieveBalance({
     *         before: "2571",
     *         after: "8516",
     *         limit: 1,
     *         merchantId: "4525644354"
     *     })
     */
    public retrieveBalance(
        request: Payroc.funding.RetrieveBalanceFundingActivityRequest = {},
        requestOptions?: FundingActivityClient.RequestOptions,
    ): core.HttpResponsePromise<Payroc.funding.RetrieveBalanceFundingActivityResponse> {
        return core.HttpResponsePromise.fromPromise(this.__retrieveBalance(request, requestOptions));
    }

    private async __retrieveBalance(
        request: Payroc.funding.RetrieveBalanceFundingActivityRequest = {},
        requestOptions?: FundingActivityClient.RequestOptions,
    ): Promise<core.WithRawResponse<Payroc.funding.RetrieveBalanceFundingActivityResponse>> {
        const { before, after, limit, merchantId } = request;
        const _queryParams: Record<string, string | string[] | object | object[] | null> = {};
        if (before != null) {
            _queryParams.before = before;
        }

        if (after != null) {
            _queryParams.after = after;
        }

        if (limit != null) {
            _queryParams.limit = limit.toString();
        }

        if (merchantId != null) {
            _queryParams.merchantId = merchantId;
        }

        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            requestOptions?.headers,
        );
        const _response = await (this._options.fetcher ?? core.fetcher)({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    ((await core.Supplier.get(this._options.environment)) ?? environments.PayrocEnvironment.Production)
                        .api,
                "funding-balance",
            ),
            method: "GET",
            headers: _headers,
            queryParameters: { ..._queryParams, ...requestOptions?.queryParams },
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: _response.body as Payroc.funding.RetrieveBalanceFundingActivityResponse,
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Payroc.BadRequestError(_response.error.body as Payroc.FourHundred, _response.rawResponse);
                case 401:
                    throw new Payroc.UnauthorizedError(
                        _response.error.body as Payroc.FourHundredOne,
                        _response.rawResponse,
                    );
                case 403:
                    throw new Payroc.ForbiddenError(_response.error.body as unknown, _response.rawResponse);
                case 406:
                    throw new Payroc.NotAcceptableError(
                        _response.error.body as Payroc.FourHundredSix,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Payroc.InternalServerError(
                        _response.error.body as Payroc.FiveHundred,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PayrocError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "GET", "/funding-balance");
    }

    /**
     * Use this method to return a [paginated](https://docs.payroc.com/api/pagination) list of activity associated with your merchants' funding balances within a specific date range.
     *
     * Use query parameters to filter the list of results we return, for example, to view the activity for a specific merchant's funding balance.
     *
     * Our gateway returns the following information about each activity in the list:
     * - Name of the merchant who owns the funding balance.
     * -	Amount of funds added or removed from the funding balance.
     * -	Funding account that received funds from the funding balance.
     *
     * @param {Payroc.funding.ListFundingActivityRequest} request
     * @param {FundingActivityClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Payroc.BadRequestError}
     * @throws {@link Payroc.UnauthorizedError}
     * @throws {@link Payroc.ForbiddenError}
     * @throws {@link Payroc.NotAcceptableError}
     * @throws {@link Payroc.InternalServerError}
     *
     * @example
     *     await client.funding.fundingActivity.list({
     *         before: "2571",
     *         after: "8516",
     *         limit: 1,
     *         dateFrom: "2024-07-02",
     *         dateTo: "2024-07-03",
     *         merchantId: "4525644354"
     *     })
     */
    public async list(
        request: Payroc.funding.ListFundingActivityRequest,
        requestOptions?: FundingActivityClient.RequestOptions,
    ): Promise<core.PayrocPager<Payroc.ActivityRecord, Payroc.funding.ListFundingActivityResponse>> {
        const { before, after, limit, dateFrom, dateTo, merchantId } = request;
        const _queryParams: Record<string, string | string[] | object | object[] | null> = {};
        if (before != null) {
            _queryParams.before = before;
        }

        if (after != null) {
            _queryParams.after = after;
        }

        if (limit != null) {
            _queryParams.limit = limit.toString();
        }

        _queryParams.dateFrom = dateFrom;
        _queryParams.dateTo = dateTo;
        if (merchantId != null) {
            _queryParams.merchantId = merchantId;
        }

        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            requestOptions?.headers,
        );
        const _request = {
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    ((await core.Supplier.get(this._options.environment)) ?? environments.PayrocEnvironment.Production)
                        .api,
                "funding-activity",
            ),
            method: "GET",
            headers: _headers,
            queryParameters: { ..._queryParams, ...requestOptions?.queryParams },
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        };
        const _sendRequest = async (request: core.Fetcher.Args) => {
            const _response = await core.fetcher<Payroc.funding.ListFundingActivityResponse>(request);
            if (_response.ok) {
                return _response;
            }
            if (_response.error.reason === "status-code") {
                switch (_response.error.statusCode) {
                    case 400:
                        throw new Payroc.BadRequestError(
                            _response.error.body as Payroc.FourHundred,
                            _response.rawResponse,
                        );
                    case 401:
                        throw new Payroc.UnauthorizedError(
                            _response.error.body as Payroc.FourHundredOne,
                            _response.rawResponse,
                        );
                    case 403:
                        throw new Payroc.ForbiddenError(_response.error.body as unknown, _response.rawResponse);
                    case 406:
                        throw new Payroc.NotAcceptableError(
                            _response.error.body as Payroc.FourHundredSix,
                            _response.rawResponse,
                        );
                    case 500:
                        throw new Payroc.InternalServerError(
                            _response.error.body as Payroc.FiveHundred,
                            _response.rawResponse,
                        );
                    default:
                        throw new errors.PayrocError({
                            statusCode: _response.error.statusCode,
                            body: _response.error.body,
                            rawResponse: _response.rawResponse,
                        });
                }
            }
            return handleNonStatusCodeError(_response.error, _response.rawResponse, "GET", "/funding-activity");
        };
        return core.createPayrocPager<Payroc.ActivityRecord, Payroc.funding.ListFundingActivityResponse>({
            sendRequest: _sendRequest,
            initialHttpRequest: _request,
            clientOptions: this._options,
            requestOptions: requestOptions,
        });
    }
}
