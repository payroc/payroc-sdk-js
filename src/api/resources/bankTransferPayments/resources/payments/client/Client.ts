// This file was auto-generated by Fern from our API Definition.

import type { BaseClientOptions, BaseRequestOptions } from "../../../../../../BaseClient.js";
import { type NormalizedClientOptionsWithAuth, normalizeClientOptionsWithAuth } from "../../../../../../BaseClient.js";
import { mergeHeaders, mergeOnlyDefinedHeaders } from "../../../../../../core/headers.js";
import * as core from "../../../../../../core/index.js";
import * as environments from "../../../../../../environments.js";
import { handleNonStatusCodeError } from "../../../../../../errors/handleNonStatusCodeError.js";
import * as errors from "../../../../../../errors/index.js";
import * as Payroc from "../../../../../index.js";

export declare namespace PaymentsClient {
    export type Options = BaseClientOptions;

    export interface RequestOptions extends BaseRequestOptions {}
}

export class PaymentsClient {
    protected readonly _options: NormalizedClientOptionsWithAuth<PaymentsClient.Options>;

    constructor(options: PaymentsClient.Options) {
        this._options = normalizeClientOptionsWithAuth(options);
    }

    /**
     * Use this method to return a [paginated](https://docs.payroc.com/api/pagination) list of payments.
     *
     * **Note:** If you want to view the details of a specific payment and you have its paymentId, use our [Retrieve Payment](https://docs.payroc.com/api/schema/bank-transfer-payments/payments/retrieve) method.
     *
     * Use query parameters to filter the list of results that we return, for example, to search for payments for a customer, a date range, or a settlement state.
     *
     * Our gateway returns the following information about each payment in the list:
     *
     * - Order details, including the transaction amount and when it was processed.
     * - Bank account details, including the customer’s name and account number.
     * - Customer's details, including the customer’s phone number.
     * - Transaction details, including any refunds or re-presentments.
     *
     * For each transaction, we also return the paymentId and an optional secureTokenId, which you can use to perform follow-on actions.
     *
     * @param {Payroc.bankTransferPayments.ListPaymentsRequest} request
     * @param {PaymentsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Payroc.BadRequestError}
     * @throws {@link Payroc.UnauthorizedError}
     * @throws {@link Payroc.ForbiddenError}
     * @throws {@link Payroc.NotAcceptableError}
     * @throws {@link Payroc.InternalServerError}
     *
     * @example
     *     await client.bankTransferPayments.payments.list({
     *         processingTerminalId: "1234001",
     *         orderId: "OrderRef6543",
     *         nameOnAccount: "Sarah%20Hazel%20Hopper",
     *         last4: "7890",
     *         dateFrom: "2024-07-01T00:00:00Z",
     *         dateTo: "2024-07-31T23:59:59Z",
     *         settlementState: "settled",
     *         settlementDate: "2024-07-15",
     *         paymentLinkId: "JZURRJBUPS",
     *         before: "2571",
     *         after: "8516",
     *         limit: 1
     *     })
     */
    public async list(
        request: Payroc.bankTransferPayments.ListPaymentsRequest,
        requestOptions?: PaymentsClient.RequestOptions,
    ): Promise<core.PayrocPager<Payroc.BankTransferPayment, Payroc.BankTransferPaymentPaginatedList>> {
        const {
            processingTerminalId,
            orderId,
            nameOnAccount,
            last4,
            type: type_,
            status,
            dateFrom,
            dateTo,
            settlementState,
            settlementDate,
            paymentLinkId,
            before,
            after,
            limit,
        } = request;
        const _queryParams: Record<string, string | string[] | object | object[] | null> = {};
        _queryParams.processingTerminalId = processingTerminalId;
        if (orderId != null) {
            _queryParams.orderId = orderId;
        }

        if (nameOnAccount != null) {
            _queryParams.nameOnAccount = nameOnAccount;
        }

        if (last4 != null) {
            _queryParams.last4 = last4;
        }

        if (type_ != null) {
            if (Array.isArray(type_)) {
                _queryParams.type = type_.map((item) => item);
            } else {
                _queryParams.type = type_;
            }
        }

        if (status != null) {
            if (Array.isArray(status)) {
                _queryParams.status = status.map((item) => item);
            } else {
                _queryParams.status = status;
            }
        }

        if (dateFrom != null) {
            _queryParams.dateFrom = dateFrom;
        }

        if (dateTo != null) {
            _queryParams.dateTo = dateTo;
        }

        if (settlementState != null) {
            _queryParams.settlementState = settlementState;
        }

        if (settlementDate != null) {
            _queryParams.settlementDate = settlementDate;
        }

        if (paymentLinkId != null) {
            _queryParams.paymentLinkId = paymentLinkId;
        }

        if (before != null) {
            _queryParams.before = before;
        }

        if (after != null) {
            _queryParams.after = after;
        }

        if (limit != null) {
            _queryParams.limit = limit.toString();
        }

        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            requestOptions?.headers,
        );
        const _request = {
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    ((await core.Supplier.get(this._options.environment)) ?? environments.PayrocEnvironment.Production)
                        .api,
                "bank-transfer-payments",
            ),
            method: "GET",
            headers: _headers,
            queryParameters: { ..._queryParams, ...requestOptions?.queryParams },
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        };
        const _sendRequest = async (request: core.Fetcher.Args) => {
            const _response = await core.fetcher<Payroc.BankTransferPaymentPaginatedList>(request);
            if (_response.ok) {
                return _response;
            }
            if (_response.error.reason === "status-code") {
                switch (_response.error.statusCode) {
                    case 400:
                        throw new Payroc.BadRequestError(
                            _response.error.body as Payroc.FourHundred,
                            _response.rawResponse,
                        );
                    case 401:
                        throw new Payroc.UnauthorizedError(
                            _response.error.body as Payroc.FourHundredOne,
                            _response.rawResponse,
                        );
                    case 403:
                        throw new Payroc.ForbiddenError(_response.error.body as unknown, _response.rawResponse);
                    case 406:
                        throw new Payroc.NotAcceptableError(
                            _response.error.body as Payroc.FourHundredSix,
                            _response.rawResponse,
                        );
                    case 500:
                        throw new Payroc.InternalServerError(
                            _response.error.body as Payroc.FiveHundred,
                            _response.rawResponse,
                        );
                    default:
                        throw new errors.PayrocError({
                            statusCode: _response.error.statusCode,
                            body: _response.error.body,
                            rawResponse: _response.rawResponse,
                        });
                }
            }
            return handleNonStatusCodeError(_response.error, _response.rawResponse, "GET", "/bank-transfer-payments");
        };
        return core.createPayrocPager<Payroc.BankTransferPayment, Payroc.BankTransferPaymentPaginatedList>({
            sendRequest: _sendRequest,
            initialHttpRequest: _request,
            clientOptions: this._options,
            requestOptions: requestOptions,
        });
    }

    /**
     * Use this method to run a sale with a customer's bank account details.
     *
     * In the response, our gateway returns information about the bank transfer payment and a paymentId, which you need for the following methods:
     * -	[Retrieve payment](https://docs.payroc.com/api/schema/bank-transfer-payments/payments/retrieve) - View the details of the bank transfer payment.
     * -	[Reverse payment](https://docs.payroc.com/api/schema/bank-transfer-payments/refunds/reverse-payment) - Cancel the bank transfer payment if it's an open batch.
     * -	[Refund payment](https://docs.payroc.com/api/schema/bank-transfer-payments/refunds/refund) - Run a referenced refund to return funds to the customer's bank account.
     *
     * **Payment methods**
     *
     * Our gateway accepts the following payment methods:
     * -	Automated clearing house (ACH) details
     * -	Pre-authorized debit (PAD) details
     *
     * You can also use [secure tokens](https://docs.payroc.com/api/schema/payments/secure-tokens/overview) and [single-use tokens](https://docs.payroc.com/api/schema/tokenization/single-use-tokens/create) that you created from ACH details or PAD details.
     *
     * @param {Payroc.bankTransferPayments.BankTransferPaymentRequest} request
     * @param {PaymentsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Payroc.BadRequestError}
     * @throws {@link Payroc.UnauthorizedError}
     * @throws {@link Payroc.ForbiddenError}
     * @throws {@link Payroc.NotAcceptableError}
     * @throws {@link Payroc.ConflictError}
     * @throws {@link Payroc.UnsupportedMediaTypeError}
     * @throws {@link Payroc.InternalServerError}
     *
     * @example
     *     await client.bankTransferPayments.payments.create({
     *         "Idempotency-Key": "8e03978e-40d5-43e8-bc93-6894a57f9324",
     *         processingTerminalId: "1234001",
     *         order: {
     *             orderId: "OrderRef6543",
     *             description: "Large Pepperoni Pizza",
     *             amount: 4999,
     *             currency: "USD",
     *             breakdown: {
     *                 subtotal: 4347,
     *                 tip: {
     *                     type: "percentage",
     *                     percentage: 10
     *                 },
     *                 taxes: [{
     *                         type: "rate",
     *                         rate: 5,
     *                         name: "Sales Tax"
     *                     }]
     *             }
     *         },
     *         customer: {
     *             notificationLanguage: "en",
     *             contactMethods: [{
     *                     type: "email",
     *                     value: "jane.doe@example.com"
     *                 }]
     *         },
     *         credentialOnFile: {
     *             tokenize: true
     *         },
     *         paymentMethod: {
     *             type: "ach",
     *             nameOnAccount: "Shara Hazel Hopper",
     *             accountNumber: "1234567890",
     *             routingNumber: "123456789"
     *         },
     *         customFields: [{
     *                 name: "yourCustomField",
     *                 value: "abc123"
     *             }]
     *     })
     */
    public create(
        request: Payroc.bankTransferPayments.BankTransferPaymentRequest,
        requestOptions?: PaymentsClient.RequestOptions,
    ): core.HttpResponsePromise<Payroc.BankTransferPayment> {
        return core.HttpResponsePromise.fromPromise(this.__create(request, requestOptions));
    }

    private async __create(
        request: Payroc.bankTransferPayments.BankTransferPaymentRequest,
        requestOptions?: PaymentsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Payroc.BankTransferPayment>> {
        const { "Idempotency-Key": idempotencyKey, ..._body } = request;
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({ "Idempotency-Key": idempotencyKey }),
            requestOptions?.headers,
        );
        const _response = await (this._options.fetcher ?? core.fetcher)({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    ((await core.Supplier.get(this._options.environment)) ?? environments.PayrocEnvironment.Production)
                        .api,
                "bank-transfer-payments",
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/json",
            queryParameters: requestOptions?.queryParams,
            requestType: "json",
            body: _body,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Payroc.BankTransferPayment, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Payroc.BadRequestError(_response.error.body as Payroc.FourHundred, _response.rawResponse);
                case 401:
                    throw new Payroc.UnauthorizedError(
                        _response.error.body as Payroc.FourHundredOne,
                        _response.rawResponse,
                    );
                case 403:
                    throw new Payroc.ForbiddenError(_response.error.body as unknown, _response.rawResponse);
                case 406:
                    throw new Payroc.NotAcceptableError(
                        _response.error.body as Payroc.FourHundredSix,
                        _response.rawResponse,
                    );
                case 409:
                    throw new Payroc.ConflictError(
                        _response.error.body as Payroc.FourHundredNine,
                        _response.rawResponse,
                    );
                case 415:
                    throw new Payroc.UnsupportedMediaTypeError(
                        _response.error.body as Payroc.FourHundredFifteen,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Payroc.InternalServerError(
                        _response.error.body as Payroc.FiveHundred,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PayrocError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "POST", "/bank-transfer-payments");
    }

    /**
     * Use this method to retrieve information about a bank transfer payment.
     *
     * To retrieve a payment, you need its paymentId. Our gateway returned the paymentId in the response of the [Create Payment](https://docs.payroc.com/api/schema/bank-transfer-payments/payments/create) method.
     *
     * Note: If you don’t have the paymentId, use our [List Payments](https://docs.payroc.com/api/schema/bank-transfer-payments/payments/list) method to search for the payment.
     *
     * Our gateway returns the following information about the payment:
     *
     * -	Order details, including the transaction amount and when it was processed.
     * -	Bank account details, including the customer’s name and account number.
     * -	Customer’s details, including the customer’s phone number.
     * -	Transaction details, including any refunds or re-presentments.
     *
     * If the merchant saved the customer’s bank account details, our gateway returns a secureTokenID, which you can use to perform follow-on actions.
     *
     * @param {Payroc.bankTransferPayments.RetrievePaymentsRequest} request
     * @param {PaymentsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Payroc.BadRequestError}
     * @throws {@link Payroc.UnauthorizedError}
     * @throws {@link Payroc.ForbiddenError}
     * @throws {@link Payroc.NotFoundError}
     * @throws {@link Payroc.NotAcceptableError}
     * @throws {@link Payroc.InternalServerError}
     *
     * @example
     *     await client.bankTransferPayments.payments.retrieve({
     *         paymentId: "M2MJOG6O2Y"
     *     })
     */
    public retrieve(
        request: Payroc.bankTransferPayments.RetrievePaymentsRequest,
        requestOptions?: PaymentsClient.RequestOptions,
    ): core.HttpResponsePromise<Payroc.BankTransferPayment> {
        return core.HttpResponsePromise.fromPromise(this.__retrieve(request, requestOptions));
    }

    private async __retrieve(
        request: Payroc.bankTransferPayments.RetrievePaymentsRequest,
        requestOptions?: PaymentsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Payroc.BankTransferPayment>> {
        const { paymentId } = request;
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            requestOptions?.headers,
        );
        const _response = await (this._options.fetcher ?? core.fetcher)({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    ((await core.Supplier.get(this._options.environment)) ?? environments.PayrocEnvironment.Production)
                        .api,
                `bank-transfer-payments/${core.url.encodePathParam(paymentId)}`,
            ),
            method: "GET",
            headers: _headers,
            queryParameters: requestOptions?.queryParams,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Payroc.BankTransferPayment, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Payroc.BadRequestError(_response.error.body as Payroc.FourHundred, _response.rawResponse);
                case 401:
                    throw new Payroc.UnauthorizedError(
                        _response.error.body as Payroc.FourHundredOne,
                        _response.rawResponse,
                    );
                case 403:
                    throw new Payroc.ForbiddenError(_response.error.body as unknown, _response.rawResponse);
                case 404:
                    throw new Payroc.NotFoundError(
                        _response.error.body as Payroc.FourHundredFour,
                        _response.rawResponse,
                    );
                case 406:
                    throw new Payroc.NotAcceptableError(
                        _response.error.body as Payroc.FourHundredSix,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Payroc.InternalServerError(
                        _response.error.body as Payroc.FiveHundred,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PayrocError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/bank-transfer-payments/{paymentId}",
        );
    }

    /**
     * Use this method to re-present an ACH payment.
     *
     * To re-present a payment, you need the paymentId of the return. To get the paymentId of the return, complete the following steps:
     *
     * 1.	Use our [Retrieve Payment](https://docs.payroc.com/api/schema/bank-transfer-payments/payments/retrieve) method  to view the details of the original payment.
     * 2.	From the [returns object](https://docs.payroc.com/api/schema/bank-transfer-payments/payments/retrieve#response.body.returns) in the response, get the paymentId of the return.
     *
     * Our gateway uses the bank account details from the original payment. If you want to update the customer's bank account details, send the new bank account details in the request.
     *
     * If your request is successful, our gateway re-presents the payment.
     *
     * @param {Payroc.bankTransferPayments.Representment} request
     * @param {PaymentsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Payroc.BadRequestError}
     * @throws {@link Payroc.UnauthorizedError}
     * @throws {@link Payroc.ForbiddenError}
     * @throws {@link Payroc.NotFoundError}
     * @throws {@link Payroc.NotAcceptableError}
     * @throws {@link Payroc.ConflictError}
     * @throws {@link Payroc.UnsupportedMediaTypeError}
     * @throws {@link Payroc.InternalServerError}
     *
     * @example
     *     await client.bankTransferPayments.payments.represent({
     *         "Idempotency-Key": "8e03978e-40d5-43e8-bc93-6894a57f9324",
     *         paymentId: "M2MJOG6O2Y",
     *         paymentMethod: {
     *             type: "ach",
     *             nameOnAccount: "Shara Hazel Hopper",
     *             accountNumber: "1234567890",
     *             routingNumber: "123456789"
     *         }
     *     })
     */
    public represent(
        request: Payroc.bankTransferPayments.Representment,
        requestOptions?: PaymentsClient.RequestOptions,
    ): core.HttpResponsePromise<Payroc.BankTransferPayment> {
        return core.HttpResponsePromise.fromPromise(this.__represent(request, requestOptions));
    }

    private async __represent(
        request: Payroc.bankTransferPayments.Representment,
        requestOptions?: PaymentsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Payroc.BankTransferPayment>> {
        const { paymentId, "Idempotency-Key": idempotencyKey, ..._body } = request;
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({ "Idempotency-Key": idempotencyKey }),
            requestOptions?.headers,
        );
        const _response = await (this._options.fetcher ?? core.fetcher)({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    ((await core.Supplier.get(this._options.environment)) ?? environments.PayrocEnvironment.Production)
                        .api,
                `bank-transfer-payments/${core.url.encodePathParam(paymentId)}/represent`,
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/json",
            queryParameters: requestOptions?.queryParams,
            requestType: "json",
            body: _body,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Payroc.BankTransferPayment, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Payroc.BadRequestError(_response.error.body as Payroc.FourHundred, _response.rawResponse);
                case 401:
                    throw new Payroc.UnauthorizedError(
                        _response.error.body as Payroc.FourHundredOne,
                        _response.rawResponse,
                    );
                case 403:
                    throw new Payroc.ForbiddenError(_response.error.body as unknown, _response.rawResponse);
                case 404:
                    throw new Payroc.NotFoundError(
                        _response.error.body as Payroc.FourHundredFour,
                        _response.rawResponse,
                    );
                case 406:
                    throw new Payroc.NotAcceptableError(
                        _response.error.body as Payroc.FourHundredSix,
                        _response.rawResponse,
                    );
                case 409:
                    throw new Payroc.ConflictError(
                        _response.error.body as Payroc.FourHundredNine,
                        _response.rawResponse,
                    );
                case 415:
                    throw new Payroc.UnsupportedMediaTypeError(
                        _response.error.body as Payroc.FourHundredFifteen,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Payroc.InternalServerError(
                        _response.error.body as Payroc.FiveHundred,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PayrocError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "POST",
            "/bank-transfer-payments/{paymentId}/represent",
        );
    }
}
