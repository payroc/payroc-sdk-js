// This file was auto-generated by Fern from our API Definition.

import type * as Payroc from "../../../../../../index.js";

/**
 * @example
 *     {
 *         processingTerminalId: "1234001",
 *         customerName: "Sarah%20Hazel%20Hopper",
 *         last4: "7062",
 *         paymentPlan: "Premium%20Club",
 *         frequency: "weekly",
 *         status: "active",
 *         endDate: "2025-07-01",
 *         nextDueDate: "2024-08-01",
 *         before: "2571",
 *         after: "8516",
 *         limit: 1
 *     }
 *
 * @example
 *     {
 *         processingTerminalId: "1234001",
 *         customerName: "Sarah%20Hazel%20Hopper",
 *         last4: "7062",
 *         paymentPlan: "Premium%20Club",
 *         frequency: "weekly",
 *         status: "active",
 *         endDate: "2025-07-01",
 *         nextDueDate: "2024-08-01",
 *         before: "2571",
 *         after: "8516",
 *         limit: 1
 *     }
 */
export interface ListSubscriptionsRequest {
    /** Unique identifier that we assigned to the terminal. */
    processingTerminalId: string;
    /** Filter by the customer's name. */
    customerName?: string;
    /** Filter by the last four digits of the card or account number. */
    last4?: string;
    /** Filter by the name of the payment plan. */
    paymentPlan?: string;
    /** Filter by the frequency of subscription payments. */
    frequency?: Payroc.repeatPayments.ListSubscriptionsRequestFrequency;
    /** Filter by the current status of the subscription. */
    status?: Payroc.repeatPayments.ListSubscriptionsRequestStatus;
    /**
     * Format: `YYYY-MM-DD`
     * Filter subscriptions that end on a specific date.
     */
    endDate?: string;
    /**
     * Format: `YYYY-MM-DD`
     * Filter subscriptions by the date that the next payment is collected.
     */
    nextDueDate?: string;
    /**
     * Return the previous page of results before the value that you specify.
     *
     * You can’t send the before parameter in the same request as the after parameter.
     */
    before?: string;
    /**
     * Return the next page of results after the value that you specify.
     *
     * You can’t send the after parameter in the same request as the before parameter.
     */
    after?: string;
    /** Limit the maximum number of results that we return for each page. */
    limit?: number;
}
