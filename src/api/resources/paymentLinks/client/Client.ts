// This file was auto-generated by Fern from our API Definition.

import type { BaseClientOptions, BaseRequestOptions } from "../../../../BaseClient.js";
import { type NormalizedClientOptionsWithAuth, normalizeClientOptionsWithAuth } from "../../../../BaseClient.js";
import { mergeHeaders, mergeOnlyDefinedHeaders } from "../../../../core/headers.js";
import * as core from "../../../../core/index.js";
import * as environments from "../../../../environments.js";
import { handleNonStatusCodeError } from "../../../../errors/handleNonStatusCodeError.js";
import * as errors from "../../../../errors/index.js";
import * as Payroc from "../../../index.js";
import { SharingEventsClient } from "../resources/sharingEvents/client/Client.js";

export declare namespace PaymentLinksClient {
    export type Options = BaseClientOptions;

    export interface RequestOptions extends BaseRequestOptions {}
}

/**
 * Create and share payment links.
 */
export class PaymentLinksClient {
    protected readonly _options: NormalizedClientOptionsWithAuth<PaymentLinksClient.Options>;
    protected _sharingEvents: SharingEventsClient | undefined;

    constructor(options: PaymentLinksClient.Options) {
        this._options = normalizeClientOptionsWithAuth(options);
    }

    public get sharingEvents(): SharingEventsClient {
        return (this._sharingEvents ??= new SharingEventsClient(this._options));
    }

    /**
     * Use this method to return a [paginated](https://docs.payroc.com/api/pagination) list of payment links linked to a processing terminal.
     *
     * **Note:** If you want to view the details of a specific payment link and you have its paymentLinkId, use our [Retrieve Payment Link](https://docs.payroc.com/api/schema/payment-links/retrieve) method.
     *
     * Use query parameters to filter the list of results that we return, for example, to search for only active links or multi-use links.
     *
     * Our gateway returns the following information about each payment link in the list:
     * - **type** - Indicates whether the link can be used only once or if it can be used multiple times.
     * - **authType** - Indicates whether the transaction is a sale or a pre-authorization.
     * - **paymentMethods** - Indicates the payment method that the merchant accepts.
     * - **charge** - Indicates whether the merchant or the customer enters the amount for the transaction.
     * - **status** - Indicates if the payment link is active.
     *
     * For each payment link, we also return a paymentLinkId, which you can use for follow-on actions.
     *
     * @param {Payroc.ListPaymentLinksRequest} request
     * @param {PaymentLinksClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Payroc.BadRequestError}
     * @throws {@link Payroc.UnauthorizedError}
     * @throws {@link Payroc.ForbiddenError}
     * @throws {@link Payroc.NotFoundError}
     * @throws {@link Payroc.NotAcceptableError}
     * @throws {@link Payroc.InternalServerError}
     *
     * @example
     *     await client.paymentLinks.list({
     *         processingTerminalId: "1234001",
     *         merchantReference: "LinkRef6543",
     *         linkType: "multiUse",
     *         chargeType: "preset",
     *         status: "active",
     *         recipientName: "Sarah Hazel Hopper",
     *         recipientEmail: "sarah.hopper@example.com",
     *         createdOn: "2024-07-02",
     *         expiresOn: "2024-08-02",
     *         before: "2571",
     *         after: "8516",
     *         limit: 1
     *     })
     */
    public async list(
        request: Payroc.ListPaymentLinksRequest,
        requestOptions?: PaymentLinksClient.RequestOptions,
    ): Promise<core.PayrocPager<Payroc.PaymentLinkPaginatedListDataItem, Payroc.PaymentLinkPaginatedList>> {
        const {
            processingTerminalId,
            merchantReference,
            linkType,
            chargeType,
            status,
            recipientName,
            recipientEmail,
            createdOn,
            expiresOn,
            before,
            after,
            limit,
        } = request;
        const _queryParams: Record<string, string | string[] | object | object[] | null> = {};
        if (merchantReference != null) {
            _queryParams.merchantReference = merchantReference;
        }

        if (linkType != null) {
            _queryParams.linkType = linkType;
        }

        if (chargeType != null) {
            _queryParams.chargeType = chargeType;
        }

        if (status != null) {
            _queryParams.status = status;
        }

        if (recipientName != null) {
            _queryParams.recipientName = recipientName;
        }

        if (recipientEmail != null) {
            _queryParams.recipientEmail = recipientEmail;
        }

        if (createdOn != null) {
            _queryParams.createdOn = createdOn;
        }

        if (expiresOn != null) {
            _queryParams.expiresOn = expiresOn;
        }

        if (before != null) {
            _queryParams.before = before;
        }

        if (after != null) {
            _queryParams.after = after;
        }

        if (limit != null) {
            _queryParams.limit = limit.toString();
        }

        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            requestOptions?.headers,
        );
        const _request = {
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    ((await core.Supplier.get(this._options.environment)) ?? environments.PayrocEnvironment.Production)
                        .api,
                `processing-terminals/${core.url.encodePathParam(processingTerminalId)}/payment-links`,
            ),
            method: "GET",
            headers: _headers,
            queryParameters: { ..._queryParams, ...requestOptions?.queryParams },
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        };
        const _sendRequest = async (request: core.Fetcher.Args) => {
            const _response = await core.fetcher<Payroc.PaymentLinkPaginatedList>(request);
            if (_response.ok) {
                return _response;
            }
            if (_response.error.reason === "status-code") {
                switch (_response.error.statusCode) {
                    case 400:
                        throw new Payroc.BadRequestError(
                            _response.error.body as Payroc.FourHundred,
                            _response.rawResponse,
                        );
                    case 401:
                        throw new Payroc.UnauthorizedError(
                            _response.error.body as Payroc.FourHundredOne,
                            _response.rawResponse,
                        );
                    case 403:
                        throw new Payroc.ForbiddenError(_response.error.body as unknown, _response.rawResponse);
                    case 404:
                        throw new Payroc.NotFoundError(
                            _response.error.body as Payroc.FourHundredFour,
                            _response.rawResponse,
                        );
                    case 406:
                        throw new Payroc.NotAcceptableError(
                            _response.error.body as Payroc.FourHundredSix,
                            _response.rawResponse,
                        );
                    case 500:
                        throw new Payroc.InternalServerError(
                            _response.error.body as Payroc.FiveHundred,
                            _response.rawResponse,
                        );
                    default:
                        throw new errors.PayrocError({
                            statusCode: _response.error.statusCode,
                            body: _response.error.body,
                            rawResponse: _response.rawResponse,
                        });
                }
            }
            return handleNonStatusCodeError(
                _response.error,
                _response.rawResponse,
                "GET",
                "/processing-terminals/{processingTerminalId}/payment-links",
            );
        };
        return core.createPayrocPager<Payroc.PaymentLinkPaginatedListDataItem, Payroc.PaymentLinkPaginatedList>({
            sendRequest: _sendRequest,
            initialHttpRequest: _request,
            clientOptions: this._options,
            requestOptions: requestOptions,
        });
    }

    /**
     * Use this method to create a payment link that a customer can use to make a payment for goods or services.
     *
     * The request includes the following settings:
     * - **type** - Indicates whether the link can be used only once or if it can be used multiple times.
     * - **authType** - Indicates whether the transaction is a sale or a pre-authorization.
     * - **paymentMethod** - Indicates the payment methods that the merchant accepts.
     * - **charge** - Indicates whether the merchant or the customer enters the amount for the transaction.
     *
     * If your request is successful, our gateway returns a paymentLinkId, which you can use to perform follow-on actions.
     *
     * **Note:** To share the payment link with a customer, use our [Share Payment Link](https://docs.payroc.com/api/schema/payment-links/sharing-events/share) method.
     *
     * @param {Payroc.CreatePaymentLinksRequest} request
     * @param {PaymentLinksClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Payroc.BadRequestError}
     * @throws {@link Payroc.UnauthorizedError}
     * @throws {@link Payroc.ForbiddenError}
     * @throws {@link Payroc.NotFoundError}
     * @throws {@link Payroc.NotAcceptableError}
     * @throws {@link Payroc.ConflictError}
     * @throws {@link Payroc.UnsupportedMediaTypeError}
     * @throws {@link Payroc.InternalServerError}
     *
     * @example
     *     await client.paymentLinks.create({
     *         "Idempotency-Key": "8e03978e-40d5-43e8-bc93-6894a57f9324",
     *         processingTerminalId: "1234001",
     *         body: {
     *             type: "multiUse",
     *             merchantReference: "LinkRef6543",
     *             order: {
     *                 charge: {
     *                     type: "prompt",
     *                     currency: "AED"
     *                 }
     *             },
     *             authType: "sale",
     *             paymentMethods: ["card"]
     *         }
     *     })
     *
     * @example
     *     await client.paymentLinks.create({
     *         "Idempotency-Key": "8e03978e-40d5-43e8-bc93-6894a57f9324",
     *         processingTerminalId: "1234001",
     *         body: {
     *             type: "singleUse",
     *             merchantReference: "LinkRef7654",
     *             order: {
     *                 orderId: "OrderRef7654",
     *                 charge: {
     *                     type: "prompt",
     *                     currency: "AED"
     *                 }
     *             },
     *             authType: "sale",
     *             paymentMethods: ["card"],
     *             expiresOn: "2024-08-02"
     *         }
     *     })
     */
    public create(
        request: Payroc.CreatePaymentLinksRequest,
        requestOptions?: PaymentLinksClient.RequestOptions,
    ): core.HttpResponsePromise<Payroc.CreatePaymentLinksResponse> {
        return core.HttpResponsePromise.fromPromise(this.__create(request, requestOptions));
    }

    private async __create(
        request: Payroc.CreatePaymentLinksRequest,
        requestOptions?: PaymentLinksClient.RequestOptions,
    ): Promise<core.WithRawResponse<Payroc.CreatePaymentLinksResponse>> {
        const { processingTerminalId, "Idempotency-Key": idempotencyKey, body: _body } = request;
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({ "Idempotency-Key": idempotencyKey }),
            requestOptions?.headers,
        );
        const _response = await (this._options.fetcher ?? core.fetcher)({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    ((await core.Supplier.get(this._options.environment)) ?? environments.PayrocEnvironment.Production)
                        .api,
                `processing-terminals/${core.url.encodePathParam(processingTerminalId)}/payment-links`,
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/json",
            queryParameters: requestOptions?.queryParams,
            requestType: "json",
            body: _body,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Payroc.CreatePaymentLinksResponse, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Payroc.BadRequestError(_response.error.body as Payroc.FourHundred, _response.rawResponse);
                case 401:
                    throw new Payroc.UnauthorizedError(
                        _response.error.body as Payroc.FourHundredOne,
                        _response.rawResponse,
                    );
                case 403:
                    throw new Payroc.ForbiddenError(_response.error.body as unknown, _response.rawResponse);
                case 404:
                    throw new Payroc.NotFoundError(
                        _response.error.body as Payroc.FourHundredFour,
                        _response.rawResponse,
                    );
                case 406:
                    throw new Payroc.NotAcceptableError(
                        _response.error.body as Payroc.FourHundredSix,
                        _response.rawResponse,
                    );
                case 409:
                    throw new Payroc.ConflictError(
                        _response.error.body as Payroc.FourHundredNine,
                        _response.rawResponse,
                    );
                case 415:
                    throw new Payroc.UnsupportedMediaTypeError(
                        _response.error.body as Payroc.FourHundredFifteen,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Payroc.InternalServerError(
                        _response.error.body as Payroc.FiveHundred,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PayrocError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "POST",
            "/processing-terminals/{processingTerminalId}/payment-links",
        );
    }

    /**
     * Use this method to retrieve information about a payment link.
     *
     * To retrieve a payment link, you need its paymentLinkId. Our gateway returned the paymentLinkId in the response of the [Create Payment Link](https://docs.payroc.com/api/schema/payment-links/create) method.
     *
     * **Note:** If you don't have the paymentLinkId, use our [List Payment Links](https://docs.payroc.com/api/schema/payment-links/list) method to search for the payment link.
     *
     * Our gateway returns the following information about the payment link:
     * - **type** - Indicates whether the link can be used only once or if it can be used multiple times.
     * - **authType** - Indicates whether the transaction is a sale or a pre-authorization.
     * - **paymentMethods** - Indicates the payment method that the merchant accepts.
     * - **charge** - Indicates whether the merchant or the customer enters the amount for the transaction.
     * - **status** - Indicates if the payment link is active.
     *
     * @param {Payroc.RetrievePaymentLinksRequest} request
     * @param {PaymentLinksClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Payroc.BadRequestError}
     * @throws {@link Payroc.UnauthorizedError}
     * @throws {@link Payroc.ForbiddenError}
     * @throws {@link Payroc.NotFoundError}
     * @throws {@link Payroc.NotAcceptableError}
     * @throws {@link Payroc.InternalServerError}
     *
     * @example
     *     await client.paymentLinks.retrieve({
     *         paymentLinkId: "JZURRJBUPS"
     *     })
     */
    public retrieve(
        request: Payroc.RetrievePaymentLinksRequest,
        requestOptions?: PaymentLinksClient.RequestOptions,
    ): core.HttpResponsePromise<Payroc.RetrievePaymentLinksResponse> {
        return core.HttpResponsePromise.fromPromise(this.__retrieve(request, requestOptions));
    }

    private async __retrieve(
        request: Payroc.RetrievePaymentLinksRequest,
        requestOptions?: PaymentLinksClient.RequestOptions,
    ): Promise<core.WithRawResponse<Payroc.RetrievePaymentLinksResponse>> {
        const { paymentLinkId } = request;
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            requestOptions?.headers,
        );
        const _response = await (this._options.fetcher ?? core.fetcher)({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    ((await core.Supplier.get(this._options.environment)) ?? environments.PayrocEnvironment.Production)
                        .api,
                `payment-links/${core.url.encodePathParam(paymentLinkId)}`,
            ),
            method: "GET",
            headers: _headers,
            queryParameters: requestOptions?.queryParams,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Payroc.RetrievePaymentLinksResponse, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Payroc.BadRequestError(_response.error.body as Payroc.FourHundred, _response.rawResponse);
                case 401:
                    throw new Payroc.UnauthorizedError(
                        _response.error.body as Payroc.FourHundredOne,
                        _response.rawResponse,
                    );
                case 403:
                    throw new Payroc.ForbiddenError(_response.error.body as unknown, _response.rawResponse);
                case 404:
                    throw new Payroc.NotFoundError(
                        _response.error.body as Payroc.FourHundredFour,
                        _response.rawResponse,
                    );
                case 406:
                    throw new Payroc.NotAcceptableError(
                        _response.error.body as Payroc.FourHundredSix,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Payroc.InternalServerError(
                        _response.error.body as Payroc.FiveHundred,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PayrocError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/payment-links/{paymentLinkId}",
        );
    }

    /**
     * Use this method to partially update a payment link. Structure your request to follow the [RFC 6902](https://datatracker.ietf.org/doc/html/rfc6902) standard.
     *
     * To update a payment link, you need its paymentLinkId, which we sent you in the response of the [Create Payment Link](https://docs.payroc.com/api/schema/payment-links/create) method.
     *
     * **Note:** If you don't have the paymentLinkId, use our [List Payment Links](https://docs.payroc.com/api/schema/payment-links/list) method to search for the payment link.
     *
     * You can update the following properties of a multi-use link:
     * - **expiresOn parameter** - Expiration date of the link.
     * - **customLabels object** - Label for the payment button.
     * - **credentialOnFile object** - Settings for saving the customer's payment details.
     *
     * You can update the following properties of a single-use link:
     * - **expiresOn parameter** - Expiration date of the link.
     * - **authType parameter** - Transaction type of the payment link.
     * - **amount parameter** - Total amount of the transaction.
     * - **currency parameter** - Currency of the transaction.
     * - **description parameter** - Brief description of the transaction.
     * - **customLabels object** - Label for the payment button.
     * - **credentialOnFile object** - Settings for saving the customer's payment details.
     *
     * **Note:** When a merchant updates a single-use link, we update the payment URL and HTML code in the assets object. The customer can't use the original link to make a payment.
     *
     * @param {Payroc.PartiallyUpdatePaymentLinksRequest} request
     * @param {PaymentLinksClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Payroc.BadRequestError}
     * @throws {@link Payroc.UnauthorizedError}
     * @throws {@link Payroc.ForbiddenError}
     * @throws {@link Payroc.NotFoundError}
     * @throws {@link Payroc.NotAcceptableError}
     * @throws {@link Payroc.ConflictError}
     * @throws {@link Payroc.UnsupportedMediaTypeError}
     * @throws {@link Payroc.InternalServerError}
     *
     * @example
     *     await client.paymentLinks.partiallyUpdate({
     *         "Idempotency-Key": "8e03978e-40d5-43e8-bc93-6894a57f9324",
     *         paymentLinkId: "JZURRJBUPS",
     *         body: [{
     *                 op: "remove",
     *                 path: "path"
     *             }]
     *     })
     *
     * @example
     *     await client.paymentLinks.partiallyUpdate({
     *         "Idempotency-Key": "8e03978e-40d5-43e8-bc93-6894a57f9324",
     *         paymentLinkId: "JZURRJBUPS",
     *         body: [{
     *                 op: "remove",
     *                 path: "path"
     *             }, {
     *                 op: "remove",
     *                 path: "path"
     *             }, {
     *                 op: "remove",
     *                 path: "path"
     *             }, {
     *                 op: "move",
     *                 from: "from",
     *                 path: "path"
     *             }, {
     *                 op: "copy",
     *                 from: "from",
     *                 path: "path"
     *             }, {
     *                 op: "remove",
     *                 path: "path"
     *             }]
     *     })
     */
    public partiallyUpdate(
        request: Payroc.PartiallyUpdatePaymentLinksRequest,
        requestOptions?: PaymentLinksClient.RequestOptions,
    ): core.HttpResponsePromise<Payroc.PartiallyUpdatePaymentLinksResponse> {
        return core.HttpResponsePromise.fromPromise(this.__partiallyUpdate(request, requestOptions));
    }

    private async __partiallyUpdate(
        request: Payroc.PartiallyUpdatePaymentLinksRequest,
        requestOptions?: PaymentLinksClient.RequestOptions,
    ): Promise<core.WithRawResponse<Payroc.PartiallyUpdatePaymentLinksResponse>> {
        const { paymentLinkId, "Idempotency-Key": idempotencyKey, body: _body } = request;
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({ "Idempotency-Key": idempotencyKey }),
            requestOptions?.headers,
        );
        const _response = await (this._options.fetcher ?? core.fetcher)({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    ((await core.Supplier.get(this._options.environment)) ?? environments.PayrocEnvironment.Production)
                        .api,
                `payment-links/${core.url.encodePathParam(paymentLinkId)}`,
            ),
            method: "PATCH",
            headers: _headers,
            contentType: "application/json",
            queryParameters: requestOptions?.queryParams,
            requestType: "json",
            body: _body,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: _response.body as Payroc.PartiallyUpdatePaymentLinksResponse,
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Payroc.BadRequestError(_response.error.body as Payroc.FourHundred, _response.rawResponse);
                case 401:
                    throw new Payroc.UnauthorizedError(
                        _response.error.body as Payroc.FourHundredOne,
                        _response.rawResponse,
                    );
                case 403:
                    throw new Payroc.ForbiddenError(_response.error.body as unknown, _response.rawResponse);
                case 404:
                    throw new Payroc.NotFoundError(
                        _response.error.body as Payroc.FourHundredFour,
                        _response.rawResponse,
                    );
                case 406:
                    throw new Payroc.NotAcceptableError(
                        _response.error.body as Payroc.FourHundredSix,
                        _response.rawResponse,
                    );
                case 409:
                    throw new Payroc.ConflictError(
                        _response.error.body as Payroc.FourHundredNine,
                        _response.rawResponse,
                    );
                case 415:
                    throw new Payroc.UnsupportedMediaTypeError(
                        _response.error.body as Payroc.FourHundredFifteen,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Payroc.InternalServerError(
                        _response.error.body as Payroc.FiveHundred,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PayrocError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "PATCH",
            "/payment-links/{paymentLinkId}",
        );
    }

    /**
     * Use this method to deactivate a payment link.
     *
     * To deactivate a payment link, you need its paymentLinkId. Our gateway returned the paymentLinkId in the response of the [Create Payment Link](https://docs.payroc.com/api/schema/payment-links/create) method.
     *
     * **Note:** If you don't have the paymentLinkId, use our [List Payment Links](https://docs.payroc.com/api/schema/payment-links/list) method to search for the payment link.
     *
     * If your request is successful, our gateway deactivates the payment link. The customer can't use the link to make a payment, and you can't reactivate the payment link.
     *
     * @param {Payroc.DeactivatePaymentLinksRequest} request
     * @param {PaymentLinksClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Payroc.BadRequestError}
     * @throws {@link Payroc.UnauthorizedError}
     * @throws {@link Payroc.ForbiddenError}
     * @throws {@link Payroc.NotFoundError}
     * @throws {@link Payroc.NotAcceptableError}
     * @throws {@link Payroc.ConflictError}
     * @throws {@link Payroc.InternalServerError}
     *
     * @example
     *     await client.paymentLinks.deactivate({
     *         paymentLinkId: "JZURRJBUPS"
     *     })
     */
    public deactivate(
        request: Payroc.DeactivatePaymentLinksRequest,
        requestOptions?: PaymentLinksClient.RequestOptions,
    ): core.HttpResponsePromise<Payroc.DeactivatePaymentLinksResponse> {
        return core.HttpResponsePromise.fromPromise(this.__deactivate(request, requestOptions));
    }

    private async __deactivate(
        request: Payroc.DeactivatePaymentLinksRequest,
        requestOptions?: PaymentLinksClient.RequestOptions,
    ): Promise<core.WithRawResponse<Payroc.DeactivatePaymentLinksResponse>> {
        const { paymentLinkId } = request;
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            requestOptions?.headers,
        );
        const _response = await (this._options.fetcher ?? core.fetcher)({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    ((await core.Supplier.get(this._options.environment)) ?? environments.PayrocEnvironment.Production)
                        .api,
                `payment-links/${core.url.encodePathParam(paymentLinkId)}/deactivate`,
            ),
            method: "POST",
            headers: _headers,
            queryParameters: requestOptions?.queryParams,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: _response.body as Payroc.DeactivatePaymentLinksResponse,
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Payroc.BadRequestError(_response.error.body as Payroc.FourHundred, _response.rawResponse);
                case 401:
                    throw new Payroc.UnauthorizedError(
                        _response.error.body as Payroc.FourHundredOne,
                        _response.rawResponse,
                    );
                case 403:
                    throw new Payroc.ForbiddenError(_response.error.body as unknown, _response.rawResponse);
                case 404:
                    throw new Payroc.NotFoundError(
                        _response.error.body as Payroc.FourHundredFour,
                        _response.rawResponse,
                    );
                case 406:
                    throw new Payroc.NotAcceptableError(
                        _response.error.body as Payroc.FourHundredSix,
                        _response.rawResponse,
                    );
                case 409:
                    throw new Payroc.ConflictError(
                        _response.error.body as Payroc.FourHundredNine,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Payroc.InternalServerError(
                        _response.error.body as Payroc.FiveHundred,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PayrocError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "POST",
            "/payment-links/{paymentLinkId}/deactivate",
        );
    }
}
