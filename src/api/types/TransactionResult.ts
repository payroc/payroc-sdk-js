// This file was auto-generated by Fern from our API Definition.

import type * as Payroc from "../index.js";

/**
 * Object that contains information about the transaction response details.
 */
export interface TransactionResult {
    /** Transaction type. */
    type?: TransactionResult.Type | undefined;
    /** Indicates the subtype of EBT in the transaction. */
    ebtType?: TransactionResult.EbtType | undefined;
    /** Current status of the transaction. */
    status: TransactionResult.Status;
    /** Authorization code that the processor assigned to the transaction. */
    approvalCode?: string | undefined;
    /**
     * Amount that the processor authorized for the transaction. This value is in the currency’s lowest denomination, for example, cents.
     *
     * **Notes:**
     * - For partial authorizations, this amount is lower than the amount in the request.
     * - If the value for **authorizedAmount** is negative, this indicates that the merchant sent funds to the customer.
     */
    authorizedAmount?: number | undefined;
    currency?: Payroc.Currency | undefined;
    /**
     * Response from the processor.
     * - `A` - The processor approved the transaction.
     * - `D` - The processor declined the transaction.
     * - `E` - The processor received the transaction but will process the transaction later.
     * - `P` - The processor authorized a portion of the original amount of the transaction.
     * - `R` - The issuer declined the transaction and indicated that the customer should contact their bank.
     * - `C` - The issuer declined the transaction and indicated that the merchant should keep the card as it was reported lost or stolen.
     */
    responseCode: TransactionResult.ResponseCode;
    /** Response description from the processor. */
    responseMessage?: string | undefined;
    /** Original response code that the processor sent. */
    processorResponseCode?: string | undefined;
    /** Identifier that the card brand assigns to the payment instruction. */
    cardSchemeReferenceId?: string | undefined;
}

export namespace TransactionResult {
    /** Transaction type. */
    export const Type = {
        Sale: "sale",
        Refund: "refund",
        PreAuthorization: "preAuthorization",
        PreAuthorizationCompletion: "preAuthorizationCompletion",
    } as const;
    export type Type = (typeof Type)[keyof typeof Type];
    /** Indicates the subtype of EBT in the transaction. */
    export const EbtType = {
        CashPurchase: "cashPurchase",
        CashPurchaseWithCashback: "cashPurchaseWithCashback",
        FoodStampPurchase: "foodStampPurchase",
        FoodStampVoucherPurchase: "foodStampVoucherPurchase",
        FoodStampReturn: "foodStampReturn",
        FoodStampVoucherReturn: "foodStampVoucherReturn",
        CashBalanceInquiry: "cashBalanceInquiry",
        FoodStampBalanceInquiry: "foodStampBalanceInquiry",
        CashWithdrawal: "cashWithdrawal",
    } as const;
    export type EbtType = (typeof EbtType)[keyof typeof EbtType];
    /** Current status of the transaction. */
    export const Status = {
        Ready: "ready",
        Pending: "pending",
        Declined: "declined",
        Complete: "complete",
        Referral: "referral",
        Pickup: "pickup",
        Reversal: "reversal",
        Admin: "admin",
        Expired: "expired",
        Accepted: "accepted",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
    /**
     * Response from the processor.
     * - `A` - The processor approved the transaction.
     * - `D` - The processor declined the transaction.
     * - `E` - The processor received the transaction but will process the transaction later.
     * - `P` - The processor authorized a portion of the original amount of the transaction.
     * - `R` - The issuer declined the transaction and indicated that the customer should contact their bank.
     * - `C` - The issuer declined the transaction and indicated that the merchant should keep the card as it was reported lost or stolen.
     */
    export const ResponseCode = {
        A: "A",
        D: "D",
        E: "E",
        P: "P",
        R: "R",
        C: "C",
    } as const;
    export type ResponseCode = (typeof ResponseCode)[keyof typeof ResponseCode];
}
