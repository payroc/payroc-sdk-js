// This file was auto-generated by Fern from our API Definition.

/**
 * Object that contains the gateway settings for the solution.
 */
export interface PayrocGateway {
    /** Name of the gateway that processes the transactions. */
    gateway: PayrocGateway.Gateway;
    /** Unique identifier of the gateway terminal template. */
    terminalTemplateId: string;
}

export namespace PayrocGateway {
    /** Name of the gateway that processes the transactions. */
    export const Gateway = {
        Payroc: "payroc",
    } as const;
    export type Gateway = (typeof Gateway)[keyof typeof Gateway];
}
