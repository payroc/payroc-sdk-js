// This file was auto-generated by Fern from our API Definition.

import * as Payroc from "../../../src/api/index";
import { PayrocClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";
import { mockBearer } from "../mockAuth";

describe("PaymentInstructionsClient", () => {
    test("submit (1)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });
        const rawRequestBody = {
            operator: "Jane",
            processingTerminalId: "1234001",
            order: { orderId: "OrderRef6543", amount: 4999, currency: "USD" },
            customizationOptions: { entryMethod: "deviceRead" },
            autoCapture: true,
        };
        const rawResponseBody = {
            status: "inProgress",
            errorMessage: "errorMessage",
            link: {
                rel: "self",
                method: "GET",
                href: "https://api.payroc.com/v1/payment-instructions/a37439165d134678a9100ebba3b29597",
            },
            paymentInstructionId: "a37439165d134678a9100ebba3b29597",
        };
        server
            .mockEndpoint()
            .post("/devices/1850010868/payment-instructions")
            .header("Idempotency-Key", "8e03978e-40d5-43e8-bc93-6894a57f9324")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.payrocCloud.paymentInstructions.submit({
            "Idempotency-Key": "8e03978e-40d5-43e8-bc93-6894a57f9324",
            serialNumber: "1850010868",
            operator: "Jane",
            processingTerminalId: "1234001",
            order: {
                orderId: "OrderRef6543",
                amount: 4999,
                currency: "USD",
            },
            customizationOptions: {
                entryMethod: "deviceRead",
            },
            autoCapture: true,
        });
        expect(response).toEqual({
            status: "inProgress",
            errorMessage: "errorMessage",
            link: {
                rel: "self",
                method: "GET",
                href: "https://api.payroc.com/v1/payment-instructions/a37439165d134678a9100ebba3b29597",
            },
            paymentInstructionId: "a37439165d134678a9100ebba3b29597",
        });
    });

    test("submit (2)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });
        const rawRequestBody = {
            processingTerminalId: "buzz",
            order: { orderId: "x", amount: 1000000, currency: "AED" },
        };
        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .post("/devices/serialNumber/payment-instructions")
            .header("Idempotency-Key", "idempotencyKey")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.paymentInstructions.submit({
                "Idempotency-Key": "idempotencyKey",
                serialNumber: "serialNumber",
                processingTerminalId: "buzz",
                order: {
                    orderId: "x",
                    amount: 1000000,
                    currency: "AED",
                },
            });
        }).rejects.toThrow(Payroc.BadRequestError);
    });

    test("submit (3)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });
        const rawRequestBody = {
            processingTerminalId: "buzz",
            order: { orderId: "x", amount: 1000000, currency: "AED" },
        };
        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .post("/devices/serialNumber/payment-instructions")
            .header("Idempotency-Key", "idempotencyKey")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.paymentInstructions.submit({
                "Idempotency-Key": "idempotencyKey",
                serialNumber: "serialNumber",
                processingTerminalId: "buzz",
                order: {
                    orderId: "x",
                    amount: 1000000,
                    currency: "AED",
                },
            });
        }).rejects.toThrow(Payroc.UnauthorizedError);
    });

    test("submit (4)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });
        const rawRequestBody = {
            processingTerminalId: "buzz",
            order: { orderId: "x", amount: 1000000, currency: "AED" },
        };
        const rawResponseBody = { key: "value" };
        server
            .mockEndpoint()
            .post("/devices/serialNumber/payment-instructions")
            .header("Idempotency-Key", "idempotencyKey")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.paymentInstructions.submit({
                "Idempotency-Key": "idempotencyKey",
                serialNumber: "serialNumber",
                processingTerminalId: "buzz",
                order: {
                    orderId: "x",
                    amount: 1000000,
                    currency: "AED",
                },
            });
        }).rejects.toThrow(Payroc.ForbiddenError);
    });

    test("submit (5)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });
        const rawRequestBody = {
            processingTerminalId: "buzz",
            order: { orderId: "x", amount: 1000000, currency: "AED" },
        };
        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .post("/devices/serialNumber/payment-instructions")
            .header("Idempotency-Key", "idempotencyKey")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.paymentInstructions.submit({
                "Idempotency-Key": "idempotencyKey",
                serialNumber: "serialNumber",
                processingTerminalId: "buzz",
                order: {
                    orderId: "x",
                    amount: 1000000,
                    currency: "AED",
                },
            });
        }).rejects.toThrow(Payroc.NotFoundError);
    });

    test("submit (6)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });
        const rawRequestBody = {
            processingTerminalId: "buzz",
            order: { orderId: "x", amount: 1000000, currency: "AED" },
        };
        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .post("/devices/serialNumber/payment-instructions")
            .header("Idempotency-Key", "idempotencyKey")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(406)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.paymentInstructions.submit({
                "Idempotency-Key": "idempotencyKey",
                serialNumber: "serialNumber",
                processingTerminalId: "buzz",
                order: {
                    orderId: "x",
                    amount: 1000000,
                    currency: "AED",
                },
            });
        }).rejects.toThrow(Payroc.NotAcceptableError);
    });

    test("submit (7)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });
        const rawRequestBody = {
            processingTerminalId: "buzz",
            order: { orderId: "x", amount: 1000000, currency: "AED" },
        };
        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .post("/devices/serialNumber/payment-instructions")
            .header("Idempotency-Key", "idempotencyKey")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(409)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.paymentInstructions.submit({
                "Idempotency-Key": "idempotencyKey",
                serialNumber: "serialNumber",
                processingTerminalId: "buzz",
                order: {
                    orderId: "x",
                    amount: 1000000,
                    currency: "AED",
                },
            });
        }).rejects.toThrow(Payroc.ConflictError);
    });

    test("submit (8)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });
        const rawRequestBody = {
            processingTerminalId: "buzz",
            order: { orderId: "x", amount: 1000000, currency: "AED" },
        };
        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .post("/devices/serialNumber/payment-instructions")
            .header("Idempotency-Key", "idempotencyKey")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(415)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.paymentInstructions.submit({
                "Idempotency-Key": "idempotencyKey",
                serialNumber: "serialNumber",
                processingTerminalId: "buzz",
                order: {
                    orderId: "x",
                    amount: 1000000,
                    currency: "AED",
                },
            });
        }).rejects.toThrow(Payroc.UnsupportedMediaTypeError);
    });

    test("submit (9)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });
        const rawRequestBody = {
            processingTerminalId: "buzz",
            order: { orderId: "x", amount: 1000000, currency: "AED" },
        };
        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .post("/devices/serialNumber/payment-instructions")
            .header("Idempotency-Key", "idempotencyKey")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(500)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.paymentInstructions.submit({
                "Idempotency-Key": "idempotencyKey",
                serialNumber: "serialNumber",
                processingTerminalId: "buzz",
                order: {
                    orderId: "x",
                    amount: 1000000,
                    currency: "AED",
                },
            });
        }).rejects.toThrow(Payroc.InternalServerError);
    });

    test("retrieve (1)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = {
            status: "completed",
            errorMessage: "errorMessage",
            link: { rel: "payment", method: "GET", href: "https://api.payroc.com/v1/payments/M2MJOG6O2Y" },
            paymentInstructionId: "a37439165d134678a9100ebba3b29597",
        };
        server
            .mockEndpoint()
            .get("/payment-instructions/e743a9165d134678a9100ebba3b29597")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.payrocCloud.paymentInstructions.retrieve({
            paymentInstructionId: "e743a9165d134678a9100ebba3b29597",
        });
        expect(response).toEqual({
            status: "completed",
            errorMessage: "errorMessage",
            link: {
                rel: "payment",
                method: "GET",
                href: "https://api.payroc.com/v1/payments/M2MJOG6O2Y",
            },
            paymentInstructionId: "a37439165d134678a9100ebba3b29597",
        });
    });

    test("retrieve (2)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .get("/payment-instructions/paymentInstructionId")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.paymentInstructions.retrieve({
                paymentInstructionId: "paymentInstructionId",
            });
        }).rejects.toThrow(Payroc.BadRequestError);
    });

    test("retrieve (3)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .get("/payment-instructions/paymentInstructionId")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.paymentInstructions.retrieve({
                paymentInstructionId: "paymentInstructionId",
            });
        }).rejects.toThrow(Payroc.UnauthorizedError);
    });

    test("retrieve (4)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = { key: "value" };
        server
            .mockEndpoint()
            .get("/payment-instructions/paymentInstructionId")
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.paymentInstructions.retrieve({
                paymentInstructionId: "paymentInstructionId",
            });
        }).rejects.toThrow(Payroc.ForbiddenError);
    });

    test("retrieve (5)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .get("/payment-instructions/paymentInstructionId")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.paymentInstructions.retrieve({
                paymentInstructionId: "paymentInstructionId",
            });
        }).rejects.toThrow(Payroc.NotFoundError);
    });

    test("retrieve (6)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .get("/payment-instructions/paymentInstructionId")
            .respondWith()
            .statusCode(406)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.paymentInstructions.retrieve({
                paymentInstructionId: "paymentInstructionId",
            });
        }).rejects.toThrow(Payroc.NotAcceptableError);
    });

    test("retrieve (7)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .get("/payment-instructions/paymentInstructionId")
            .respondWith()
            .statusCode(500)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.paymentInstructions.retrieve({
                paymentInstructionId: "paymentInstructionId",
            });
        }).rejects.toThrow(Payroc.InternalServerError);
    });

    test("delete (1)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        server
            .mockEndpoint()
            .delete("/payment-instructions/e743a9165d134678a9100ebba3b29597")
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.payrocCloud.paymentInstructions.delete({
            paymentInstructionId: "e743a9165d134678a9100ebba3b29597",
        });
        expect(response).toEqual(undefined);
    });

    test("delete (2)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .delete("/payment-instructions/paymentInstructionId")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.paymentInstructions.delete({
                paymentInstructionId: "paymentInstructionId",
            });
        }).rejects.toThrow(Payroc.BadRequestError);
    });

    test("delete (3)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .delete("/payment-instructions/paymentInstructionId")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.paymentInstructions.delete({
                paymentInstructionId: "paymentInstructionId",
            });
        }).rejects.toThrow(Payroc.UnauthorizedError);
    });

    test("delete (4)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = { key: "value" };
        server
            .mockEndpoint()
            .delete("/payment-instructions/paymentInstructionId")
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.paymentInstructions.delete({
                paymentInstructionId: "paymentInstructionId",
            });
        }).rejects.toThrow(Payroc.ForbiddenError);
    });

    test("delete (5)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .delete("/payment-instructions/paymentInstructionId")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.paymentInstructions.delete({
                paymentInstructionId: "paymentInstructionId",
            });
        }).rejects.toThrow(Payroc.NotFoundError);
    });

    test("delete (6)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .delete("/payment-instructions/paymentInstructionId")
            .respondWith()
            .statusCode(406)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.paymentInstructions.delete({
                paymentInstructionId: "paymentInstructionId",
            });
        }).rejects.toThrow(Payroc.NotAcceptableError);
    });

    test("delete (7)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .delete("/payment-instructions/paymentInstructionId")
            .respondWith()
            .statusCode(409)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.paymentInstructions.delete({
                paymentInstructionId: "paymentInstructionId",
            });
        }).rejects.toThrow(Payroc.ConflictError);
    });

    test("delete (8)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .delete("/payment-instructions/paymentInstructionId")
            .respondWith()
            .statusCode(500)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.paymentInstructions.delete({
                paymentInstructionId: "paymentInstructionId",
            });
        }).rejects.toThrow(Payroc.InternalServerError);
    });
});
