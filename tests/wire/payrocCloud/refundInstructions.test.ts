// This file was auto-generated by Fern from our API Definition.

import * as Payroc from "../../../src/api/index";
import { PayrocClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";
import { mockBearer } from "../mockAuth";

describe("RefundInstructionsClient", () => {
    test("submit (1)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });
        const rawRequestBody = {
            operator: "Jane",
            processingTerminalId: "1234001",
            order: {
                orderId: "OrderRef6543",
                description: "Refund for order OrderRef6543",
                amount: 4999,
                currency: "USD",
            },
            customizationOptions: { entryMethod: "manualEntry" },
        };
        const rawResponseBody = {
            status: "inProgress",
            errorMessage: "errorMessage",
            link: {
                rel: "self",
                method: "GET",
                href: "https://api.payroc.com/v1/refund-instructions/a37439165d134678a9100ebba3b29597",
            },
            refundInstructionId: "a37439165d134678a9100ebba3b29597",
        };
        server
            .mockEndpoint()
            .post("/devices/1850010868/refund-instructions")
            .header("Idempotency-Key", "8e03978e-40d5-43e8-bc93-6894a57f9324")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.payrocCloud.refundInstructions.submit({
            "Idempotency-Key": "8e03978e-40d5-43e8-bc93-6894a57f9324",
            serialNumber: "1850010868",
            operator: "Jane",
            processingTerminalId: "1234001",
            order: {
                orderId: "OrderRef6543",
                description: "Refund for order OrderRef6543",
                amount: 4999,
                currency: "USD",
            },
            customizationOptions: {
                entryMethod: "manualEntry",
            },
        });
        expect(response).toEqual({
            status: "inProgress",
            errorMessage: "errorMessage",
            link: {
                rel: "self",
                method: "GET",
                href: "https://api.payroc.com/v1/refund-instructions/a37439165d134678a9100ebba3b29597",
            },
            refundInstructionId: "a37439165d134678a9100ebba3b29597",
        });
    });

    test("submit (2)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });
        const rawRequestBody = {
            processingTerminalId: "buzz",
            order: { orderId: "x", amount: 1000000, currency: "AED" },
        };
        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .post("/devices/serialNumber/refund-instructions")
            .header("Idempotency-Key", "idempotencyKey")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.refundInstructions.submit({
                "Idempotency-Key": "idempotencyKey",
                serialNumber: "serialNumber",
                processingTerminalId: "buzz",
                order: {
                    orderId: "x",
                    amount: 1000000,
                    currency: "AED",
                },
            });
        }).rejects.toThrow(Payroc.BadRequestError);
    });

    test("submit (3)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });
        const rawRequestBody = {
            processingTerminalId: "buzz",
            order: { orderId: "x", amount: 1000000, currency: "AED" },
        };
        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .post("/devices/serialNumber/refund-instructions")
            .header("Idempotency-Key", "idempotencyKey")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.refundInstructions.submit({
                "Idempotency-Key": "idempotencyKey",
                serialNumber: "serialNumber",
                processingTerminalId: "buzz",
                order: {
                    orderId: "x",
                    amount: 1000000,
                    currency: "AED",
                },
            });
        }).rejects.toThrow(Payroc.UnauthorizedError);
    });

    test("submit (4)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });
        const rawRequestBody = {
            processingTerminalId: "buzz",
            order: { orderId: "x", amount: 1000000, currency: "AED" },
        };
        const rawResponseBody = { key: "value" };
        server
            .mockEndpoint()
            .post("/devices/serialNumber/refund-instructions")
            .header("Idempotency-Key", "idempotencyKey")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.refundInstructions.submit({
                "Idempotency-Key": "idempotencyKey",
                serialNumber: "serialNumber",
                processingTerminalId: "buzz",
                order: {
                    orderId: "x",
                    amount: 1000000,
                    currency: "AED",
                },
            });
        }).rejects.toThrow(Payroc.ForbiddenError);
    });

    test("submit (5)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });
        const rawRequestBody = {
            processingTerminalId: "buzz",
            order: { orderId: "x", amount: 1000000, currency: "AED" },
        };
        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .post("/devices/serialNumber/refund-instructions")
            .header("Idempotency-Key", "idempotencyKey")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.refundInstructions.submit({
                "Idempotency-Key": "idempotencyKey",
                serialNumber: "serialNumber",
                processingTerminalId: "buzz",
                order: {
                    orderId: "x",
                    amount: 1000000,
                    currency: "AED",
                },
            });
        }).rejects.toThrow(Payroc.NotFoundError);
    });

    test("submit (6)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });
        const rawRequestBody = {
            processingTerminalId: "buzz",
            order: { orderId: "x", amount: 1000000, currency: "AED" },
        };
        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .post("/devices/serialNumber/refund-instructions")
            .header("Idempotency-Key", "idempotencyKey")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(406)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.refundInstructions.submit({
                "Idempotency-Key": "idempotencyKey",
                serialNumber: "serialNumber",
                processingTerminalId: "buzz",
                order: {
                    orderId: "x",
                    amount: 1000000,
                    currency: "AED",
                },
            });
        }).rejects.toThrow(Payroc.NotAcceptableError);
    });

    test("submit (7)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });
        const rawRequestBody = {
            processingTerminalId: "buzz",
            order: { orderId: "x", amount: 1000000, currency: "AED" },
        };
        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .post("/devices/serialNumber/refund-instructions")
            .header("Idempotency-Key", "idempotencyKey")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(409)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.refundInstructions.submit({
                "Idempotency-Key": "idempotencyKey",
                serialNumber: "serialNumber",
                processingTerminalId: "buzz",
                order: {
                    orderId: "x",
                    amount: 1000000,
                    currency: "AED",
                },
            });
        }).rejects.toThrow(Payroc.ConflictError);
    });

    test("submit (8)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });
        const rawRequestBody = {
            processingTerminalId: "buzz",
            order: { orderId: "x", amount: 1000000, currency: "AED" },
        };
        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .post("/devices/serialNumber/refund-instructions")
            .header("Idempotency-Key", "idempotencyKey")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(415)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.refundInstructions.submit({
                "Idempotency-Key": "idempotencyKey",
                serialNumber: "serialNumber",
                processingTerminalId: "buzz",
                order: {
                    orderId: "x",
                    amount: 1000000,
                    currency: "AED",
                },
            });
        }).rejects.toThrow(Payroc.UnsupportedMediaTypeError);
    });

    test("submit (9)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });
        const rawRequestBody = {
            processingTerminalId: "buzz",
            order: { orderId: "x", amount: 1000000, currency: "AED" },
        };
        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .post("/devices/serialNumber/refund-instructions")
            .header("Idempotency-Key", "idempotencyKey")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(500)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.refundInstructions.submit({
                "Idempotency-Key": "idempotencyKey",
                serialNumber: "serialNumber",
                processingTerminalId: "buzz",
                order: {
                    orderId: "x",
                    amount: 1000000,
                    currency: "AED",
                },
            });
        }).rejects.toThrow(Payroc.InternalServerError);
    });

    test("retrieve (1)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = {
            status: "completed",
            errorMessage: "errorMessage",
            link: { rel: "refund", method: "GET", href: "https://api.payroc.com/v1/refunds/CD3HN88U9F" },
            refundInstructionId: "a37439165d134678a9100ebba3b29597",
        };
        server
            .mockEndpoint()
            .get("/refund-instructions/a37439165d134678a9100ebba3b29597")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.payrocCloud.refundInstructions.retrieve({
            refundInstructionId: "a37439165d134678a9100ebba3b29597",
        });
        expect(response).toEqual({
            status: "completed",
            errorMessage: "errorMessage",
            link: {
                rel: "refund",
                method: "GET",
                href: "https://api.payroc.com/v1/refunds/CD3HN88U9F",
            },
            refundInstructionId: "a37439165d134678a9100ebba3b29597",
        });
    });

    test("retrieve (2)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .get("/refund-instructions/refundInstructionId")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.refundInstructions.retrieve({
                refundInstructionId: "refundInstructionId",
            });
        }).rejects.toThrow(Payroc.BadRequestError);
    });

    test("retrieve (3)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .get("/refund-instructions/refundInstructionId")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.refundInstructions.retrieve({
                refundInstructionId: "refundInstructionId",
            });
        }).rejects.toThrow(Payroc.UnauthorizedError);
    });

    test("retrieve (4)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = { key: "value" };
        server
            .mockEndpoint()
            .get("/refund-instructions/refundInstructionId")
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.refundInstructions.retrieve({
                refundInstructionId: "refundInstructionId",
            });
        }).rejects.toThrow(Payroc.ForbiddenError);
    });

    test("retrieve (5)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .get("/refund-instructions/refundInstructionId")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.refundInstructions.retrieve({
                refundInstructionId: "refundInstructionId",
            });
        }).rejects.toThrow(Payroc.NotFoundError);
    });

    test("retrieve (6)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .get("/refund-instructions/refundInstructionId")
            .respondWith()
            .statusCode(406)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.refundInstructions.retrieve({
                refundInstructionId: "refundInstructionId",
            });
        }).rejects.toThrow(Payroc.NotAcceptableError);
    });

    test("retrieve (7)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .get("/refund-instructions/refundInstructionId")
            .respondWith()
            .statusCode(500)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.refundInstructions.retrieve({
                refundInstructionId: "refundInstructionId",
            });
        }).rejects.toThrow(Payroc.InternalServerError);
    });

    test("delete (1)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        server
            .mockEndpoint()
            .delete("/refund-instructions/a37439165d134678a9100ebba3b29597")
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.payrocCloud.refundInstructions.delete({
            refundInstructionId: "a37439165d134678a9100ebba3b29597",
        });
        expect(response).toEqual(undefined);
    });

    test("delete (2)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .delete("/refund-instructions/refundInstructionId")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.refundInstructions.delete({
                refundInstructionId: "refundInstructionId",
            });
        }).rejects.toThrow(Payroc.BadRequestError);
    });

    test("delete (3)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .delete("/refund-instructions/refundInstructionId")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.refundInstructions.delete({
                refundInstructionId: "refundInstructionId",
            });
        }).rejects.toThrow(Payroc.UnauthorizedError);
    });

    test("delete (4)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = { key: "value" };
        server
            .mockEndpoint()
            .delete("/refund-instructions/refundInstructionId")
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.refundInstructions.delete({
                refundInstructionId: "refundInstructionId",
            });
        }).rejects.toThrow(Payroc.ForbiddenError);
    });

    test("delete (5)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .delete("/refund-instructions/refundInstructionId")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.refundInstructions.delete({
                refundInstructionId: "refundInstructionId",
            });
        }).rejects.toThrow(Payroc.NotFoundError);
    });

    test("delete (6)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .delete("/refund-instructions/refundInstructionId")
            .respondWith()
            .statusCode(406)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.refundInstructions.delete({
                refundInstructionId: "refundInstructionId",
            });
        }).rejects.toThrow(Payroc.NotAcceptableError);
    });

    test("delete (7)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .delete("/refund-instructions/refundInstructionId")
            .respondWith()
            .statusCode(409)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.refundInstructions.delete({
                refundInstructionId: "refundInstructionId",
            });
        }).rejects.toThrow(Payroc.ConflictError);
    });

    test("delete (8)", async () => {
        const server = mockServerPool.createServer();
        mockBearer(server);

        const client = new PayrocClient({
            maxRetries: 0,
            apiKey: "x-api-key",
            environment: { api: server.baseUrl, identity: server.baseUrl },
        });

        const rawResponseBody = { type: "type", title: "title", status: 1, detail: "detail" };
        server
            .mockEndpoint()
            .delete("/refund-instructions/refundInstructionId")
            .respondWith()
            .statusCode(500)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.payrocCloud.refundInstructions.delete({
                refundInstructionId: "refundInstructionId",
            });
        }).rejects.toThrow(Payroc.InternalServerError);
    });
});
